import React from "react";

const Loader = ({style}) => {
    return (
        <div style={{zIndex: '10000', width: '100%', height: '100%'}} className="bg-light">
            {/*<img src={} alt={''}/>*/}
        </div>
    )
}

export default Loader;